@using FSH.Starter.Blazor.Modules.Document.Blazor.Components.FileExplorer.Components.InlineForms
@using FSH.Starter.Blazor.Modules.Document.Blazor.Components.FileExplorer.Dialogs
@using FSH.Starter.Blazor.Modules.Document.Blazor.Components.FileExplorer.Models
@using FSH.Starter.Blazor.Modules.Document.Blazor.Components.FileExplorer.Services.Interfaces
@using Microsoft.AspNetCore.Components.Forms
@using MudBlazor
@implements System.IDisposable


<CascadingValue Value="@Folders" Name="Folders">
  <CascadingValue Value="@InstanceId" Name="InstanceId">
    <CascadingValue Value="@CurrentFolder" Name="CurrentFolder">

      @* <MudPaper Style="overflow:hidden; position:relative;min-height:500px;" Elevation="8"> *@
      <div style="display: flex; flex-direction: column; height: 100vh;min-height: 100vh;">

        <!-- Item 1: Auto Height -->
        <div style="flex: 0 1 auto;">
          <MudStack Spacing="0">
            <MudToolBar Dense="true" Class="mx-0 ps-2 pe-0" Style="border: 1px solid var(--mud-palette-divider); background-color: var(--mud-palette-background-grey);width:100%;">
              <!-- 4 Navigation Icons -->
              <MudIconButton Class="me-6" Icon="@Icons.Material.Filled.ArrowBack" Size="Size.Small" />
              <MudIconButton Class="me-6" Icon="@Icons.Material.Filled.ArrowForward" Size="Size.Small" />
              <MudIconButton Class="me-6" Icon="@Icons.Material.Filled.ArrowUpward" Size="Size.Small" />
              <MudIconButton Icon="@Icons.Material.Filled.Refresh" Size="Size.Small" />

              <!-- Path / Address Bar (Expands dynamically) -->
              <div class="flex-grow-1 mx-4">
                <div class="mud-input-slot mud-input-root-outlined mud-input-root-adorned-end" style="margin:4px;padding:0;color: currentcolor;border-color: currentcolor;-webkit-tap-highlight-color: rgba(0, 0, 0, 0);">
                  <ExplorerAddressBreadcrumbs CurrentPath="@CurrentFolder.Path" CurrentPathChanged="OnCurrentPathChanged" />
                </div>
              </div>

              <!-- Search Bar (Fixed structural width) -->
              <div style="width: 290px; min-width: 150px;">
                <MudTextField T="string" IconSize="Size.Small" Typo="Typo.caption"
                              @bind-Value="Search" Placeholder="Search..."
                              Variant="Variant.Outlined"
                              Margin="Margin.Dense"
                              Adornment="Adornment.End"
                              AdornmentIcon="@Icons.Material.Filled.Search"
                              FullWidth="true"
                              Style="margin-inline-end:0.5rem;" />
              </div>
            </MudToolBar>
            <MudToolBar Dense="true" Class="mx-0 ps-2 pe-0" Style="border: 1px solid var(--mud-palette-divider); background-color: var(--mud-palette-background-grey);width:100%;z-index: 1250; overflow: visible;">
              @*-------------------- New Menu --------------------- *@
              <MudMenu PopoverClass="no-shadow-menu" Class="file-explorer-icon-highlited" Label="New" Dense="true" Size="Size.Small" Variant="Variant.Text" StartIcon="@FileExplorerIcons.NewItemInside" EndIcon="@Icons.Material.Filled.KeyboardArrowDown" Style="color:var(--mud-palette-primary);text-transform:none">
                @*  <MudMenuItem Label="Create New Folder">
                <div class="d-flex align-center">
                  <MudIcon Icon="@Icons.Material.Filled.CreateNewFolder"
                           Style="@($"color: {"#ff9800"};padding-right:6px;")" />
                  <MudText>Create New Folder</MudText>
                </div>
              </MudMenuItem> *@
                <MudMenuItem Label="Create New Folder" Icon="@Icons.Material.Filled.CreateNewFolder" IconColor="Color.Warning" OnClick="ActionCreateFolder" />
                <MudMenuItem Label="Upload File" Icon="@Icons.Material.Filled.Upload" IconColor="Color.Warning" />
              </MudMenu>
              <MudIconButton class="action-icon-button" Icon="@FileExplorerIcons.Cut" />
              <MudIconButton class="action-icon-button" Icon="@FileExplorerIcons.Copy" />
              <MudIconButton class="action-icon-button" Icon="@FileExplorerIcons.Past" />
              <MudIconButton class="action-icon-button" Icon="@FileExplorerIcons.RenameFolder" />
              <MudIconButton class="action-icon-button" Icon="@FileExplorerIcons.Share" />
              <MudIconButton class="action-icon-button" Icon="@FileExplorerIcons.Delete" />
              <MudMenu PopoverClass="no-shadow-menu" Class="file-explorer-icon-highlited" Label="Sort" Dense="true" Size="Size.Small" Variant="Variant.Text" StartIcon="@FileExplorerIcons.SortingASC" EndIcon="@Icons.Material.Filled.KeyboardArrowDown" Style="color:var(--mud-palette-primary);text-transform:none">
                <MudMenuItem Label="Create New Folder">
                  <div class="d-flex align-center">
                    <MudIcon Icon="@Icons.Material.Filled.CreateNewFolder"
                             Style="@($"color: {"#ff9800"};padding-right:6px;")" />
                    <MudText>Create New Folder</MudText>
                  </div>
                </MudMenuItem><MudMenuItem Label="Item 3" />
              </MudMenu>
              <MudMenu PopoverClass="no-shadow-menu" Class="file-explorer-icon-highlited" Label="View" Dense="true" Size="Size.Small" Variant="Variant.Text" StartIcon="@FileExplorerIcons.View" EndIcon="@Icons.Material.Filled.KeyboardArrowDown" Style="color:var(--mud-palette-primary);text-transform:none">
                <MudMenuItem Label="Create New Folder">
                  <div class="d-flex align-center">
                    <MudIcon Icon="@Icons.Material.Filled.CreateNewFolder"
                             Style="@($"color: {"#ff9800"};padding-right:6px;")" />
                    <MudText>Create New Folder</MudText>
                  </div>
                </MudMenuItem>
                <MudMenuItem Label="Item 3" />
              </MudMenu>
              <MudButton Class="action-icon-button" Variant="Variant.Text" StartIcon="@FileExplorerIcons.CompressFolder" Color="Color.Info" Size="Size.Small" Style="text-transform:none">Extract all</MudButton>

              
              <MudMenu PopoverClass="no-shadow-menu" Class="file-explorer-icon-highlited" AriaLabel="More" Dense="true" Size="Size.Small" Variant="Variant.Text" Icon="@Icons.Material.Filled.MoreHoriz" Style="color:var(--mud-palette-primary);">
                <MudMenuItem Label="Create New Folder">
                  <div class="d-flex align-center">
                    <MudIcon Icon="@Icons.Material.Filled.CreateNewFolder"
                             Style="@($"color: {"#ff9800"};padding-right:6px;")" />
                    <MudText>Create New Folder</MudText>
                  </div>
                </MudMenuItem>
                <MudMenuItem Label="Item 3" />
              </MudMenu>

              <MudSpacer />

              <MudButton OnClick="ToggleFoldersTree" Class="action-icon-button" Variant="Variant.Text" StartIcon="@FileExplorerIcons.Folders.ThisPc" Color="Color.Info" Size="Size.Medium" Style="text-transform:none">Details</MudButton>

            </MudToolBar>
          </MudStack>

        </div>

        <!-- Item 2: Takes the remaining height -->
        <div style="flex: 1 1 auto; overflow-y: scroll;">
          @* ////////////////////////////////////////////////////////////////// *@
          <MudSplitPanel @ref="_splitPanel"
                         ClassDivider="mud-spliter-divider"
                         Class="mud-width-full"
                         Style="display:flex;"
                         Horizontal="_horizontal"
                         ResetOnDoubleClick="_resetOnDoubleClick"
                         Rounded="_rounded"
                         ClassFirstPanel="first-panel-wrapper d-flex"
                         FirstPanelInitialSize="250"
                         MinPanelSize="100"
                         UseAsOverlay="false"
                         Transparent="_transparent"
                         Elevation="@(_elevation ? 0 : 0)">
            <FirstPanel>
              <MudStack Style=" display:flex;flex: 1 0 auto;height:100%" Justify="Justify.Center" AlignItems="AlignItems.Center" Class="mud-width-full ">
                <MudPaper class="py-2 pe-2 mt-0 mb-0 ms-4  panel-inner-shadow mud-width-full mud-height-full" Elevation="0">
                  @* <FileExplorerFoldersListComponent OnFolderUpdated="OnFolderUpdated" OnFolderSelected="OnFolderSelected" /> *@
                  <FileExplorerFoldersListComponent />
                </MudPaper>
              </MudStack>
            </FirstPanel>
            <SecondPanel>
              @*<MudStack Justify="Justify.Center" AlignItems="AlignItems.Center" Class="mud-width-full mud-height-full"> *@
              <MudPaper Style="height:100%; position:relative; overflow:hidden;" Square="true">

                <MudDrawerContainer Style="height: 100%; width: 100%; display:flex;">
                  <MudDrawer Open="@_open" Fixed="false" Anchor="Anchor.End" Elevation="0" Variant="@DrawerVariant.Persistent" Color="Color.Primary">

                  </MudDrawer>
                  <MudPaper Class="pa-4 mt-0 mb-0 mx-0 panel-inner-shadow " Elevation="0" Style="box-sizing: border-box; flex:1 1 auto; min-width:0; height: 60vh; overflow: auto;">
                    @* <FileExplorerContentComponent OnFolderUpdated="OnFolderUpdated" OnToggleFileBrowserTree="ToggleFileBrowserTree" /> *@
                    <InlineCreateFolder></InlineCreateFolder>
                    <FileExplorerContentComponent />
                  </MudPaper>

            
                </MudDrawerContainer>
              </MudPaper>
              @* </MudStack> *@
            </SecondPanel>
          </MudSplitPanel>
        </div>

      </div>

    </CascadingValue>
  </CascadingValue>
</CascadingValue>

@code {
  //[Inject] ISnackbar Snackbar { get; set; } = default!;
  [Inject] IFileExplorerStateService StateService { get; set; } = default!;

  [Parameter] public bool AllowMultipleSelection { get; set; }

  private Guid InstanceId { get; init; } = Guid.NewGuid();
  private List<FolderModel> Folders { get; set; } = new();
  private FolderModel CurrentFolder { get; set; }//=> _currentFolder ??= Folders.First();
  // private FolderModel? _currentFolder;
  private string Search = string.Empty;
  public string SelectedFolder = string.Empty;
  private bool _open = false;
  private bool _isSubscribed = false;
  //////////////// MudSpliter
  private MudSplitPanel _splitPanel;
  private bool _horizontal;
  private bool _resetOnDoubleClick = true;
  private bool _rounded = true;
  private bool _elevation = true;
  private bool _transparent;
  bool _isFolersTreeOpen { get; set; } = true;


  #region Events
  protected override void OnInitialized()
  {
    var root = new FolderModel(Guid.NewGuid(), $"Root", new[] {
                    new FileModel(Guid.NewGuid(), $"Report.pdf", 245_760, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-2)),
                    new FileModel(Guid.NewGuid(), $"door.jpg", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"iceland.jpg", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"tractor-very-long-file-name-wraptext.jpg", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"castle.jpg", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"pilars.jpg", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"sweden.jpg", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"document_01.docx", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"document_02.doc", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"document_03.xlsx", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"document_04.xls", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"document_05.mdb", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"document_05.accdb", 1_048_576, DateTime.Now.AddDays(-2), DateTime.Now.AddDays(-10)),
                    new FileModel(Guid.NewGuid(), $"Notes.txt", 1024, DateTime.Now.AddDays(-2), DateTime.Now.AddHours(-5))
        },
           new[] {
                new FolderModel(Guid.NewGuid(), "Projects", new[] {
                            new FileModel(Guid.NewGuid(), "project1.png", 512_000, DateTime.Now.AddDays(-15), DateTime.Now.AddDays(-10)),
                            new FileModel(Guid.NewGuid(), "project2.png", 768_000, DateTime.Now.AddDays(-12), DateTime.Now.AddDays(-8))
                        } ){ Path = $"{Constants._content}/img/projects", AllowedExtensions="mpeg" },
                new FolderModel(Guid.NewGuid(), "Vacations", new[]{
                            new FileModel(Guid.NewGuid(), "beach.png", 1_024_000, DateTime.Now.AddDays(-30), DateTime.Now.AddDays(-25)),
                            new FileModel(Guid.NewGuid(), "mountains.png", 1_536_000, DateTime.Now.AddDays(-28), DateTime.Now.AddDays(-20))
                        }){Path = $"{Constants._content}/img/vacations" , AllowedExtensions="jpg" },
                new FolderModel(Guid.NewGuid(), "Docs files", new[]{
                            new FileModel(Guid.NewGuid(), "Doc01.pdf", 1_024_000, DateTime.Now.AddDays(-30), DateTime.Now.AddDays(-25)),
                            new FileModel(Guid.NewGuid(), "doc02.docx", 1_536_000, DateTime.Now.AddDays(-28), DateTime.Now.AddDays(-20))
                        }){Path = $"{Constants._content}/documents" , AllowedExtensions="pdf,docx" },
                new FolderModel(Guid.NewGuid(), "Music files", new[]{
                            new FileModel(Guid.NewGuid(), "voice.wav", 1_024_000, DateTime.Now.AddDays(-30), DateTime.Now.AddDays(-25)),
                            new FileModel(Guid.NewGuid(), "video.mp4", 1_536_000, DateTime.Now.AddDays(-28), DateTime.Now.AddDays(-20))
                        }){Path = $"{Constants._content}/music" , AllowedExtensions="mp3,mp4" }
           }

        )
    { Path = $"{Constants._content}/img/" };

    Folders.Add(root);

    var shared = new FolderModel(Guid.NewGuid(), "Shared", new[]
    {
            new FileModel(Guid.NewGuid(), "Meeting.mp4", 50_000_000, DateTime.Now.AddDays(-20), DateTime.Now.AddDays(-20))
        })
    { Path = $"{Constants._content}/Shared" };

    Folders.Add(shared);
    var archive = new FolderModel(Guid.NewGuid(), "Archive") { Path = $"{Constants._content}/archive" };

    Folders.Add(archive);


    // _currentFolder = Folders.First();
    CurrentFolder = Folders.First();
  }

  protected override async Task OnAfterRenderAsync(bool firstRender)
  {
    if (firstRender)
    {
      // DetatchActions();
      if (!_isSubscribed)
      {
        StateService.OnFileExplorerClearSelection += ClearSelection;
        StateService.OnCurrentFolderChanged += HandleOnCurrentFolderChanged;
        StateService.OnToggleFileBrowserTree += ToggleFileBrowserTree;
        // await StateService.NotifyCurrentFolderChanged(CurrentFolder);
        _isSubscribed = true;
      }

    }
  }

  #endregion

  #region Actions
  private void ActionCreateFolder()
  {
    string[] names = CurrentFolder.Folders.Select(e => e.Name.ToLowerInvariant()).Concat(CurrentFolder.Files.Select(e => e.Name.ToLowerInvariant())).ToArray();
    string newFolderName = "New Folder";
    var i = 1;
    while (names.Contains(newFolderName.ToLowerInvariant()))
    {
      newFolderName = $"New Folder ({i++})";
    }
    if (newFolderName.EndsWith("\r\n"))
    {
      newFolderName = newFolderName.Substring(0, newFolderName.Length - 2);
    }
    else if (newFolderName.EndsWith("\n"))
    {
      newFolderName = newFolderName.Substring(0, newFolderName.Length - 1);
    }
    var fldr = new FolderModel(Guid.NewGuid(), newFolderName, []);
    fldr.IsNeedsFocus = true; fldr.IsEditing = true;
    fldr.Path = $"{CurrentFolder.Path}/{newFolderName}";
    StateService.NotifyFolderCreated(InstanceId, fldr);
    StateService.NotifyFolderSelectionChanged(fldr);
  }
  #endregion
  private void ToggleFileBrowserTree(bool opened)
  {
    _open = opened;
    StateHasChanged();
    // StateService.NotifyStateChanged();
  }
  public void ToggleFoldersTree()
  {
    _isFolersTreeOpen = !_isFolersTreeOpen;
    // OnToggleFileBrowserTree.InvokeAsync(_isFolersTreeOpen);
    StateService.NotifyFileBrowserTreeToogled(_isFolersTreeOpen);
  }

  void OnFolderSelected(FolderModel folder)
  {
    // _currentFolder = folder;
    CurrentFolder = folder;
    StateHasChanged();
  }


  async Task ClearSelection()
  {
    if (!AllowMultipleSelection)
    {
      foreach (var folder in CurrentFolder.Folders)
      {
        folder.UnSelect();
      }
      foreach (var file in CurrentFolder.Files)
      {
        file.UnSelect();
      }
    }
  }
  // Optional stronger upload handler using InputFile:
  async Task OnInputFileChange(InputFileChangeEventArgs e)
  {
    foreach (var browserFile in e.GetMultipleFiles())
    {
      var model = new FileModel(Guid.NewGuid(), browserFile.Name, browserFile.Size, DateTime.Now, DateTime.Now);
      CurrentFolder.AddFile(model);
    }
    Toast.Add($"Uploaded {e.GetMultipleFiles().Count} file(s).", Severity.Success);
  }


  //async Task OnFolderUpdated(ICollection<FolderModel> updatedFolders)
  async Task HandleOnCurrentFolderChanged(FolderModel folder)
  {
    // Folders = updatedFolders.ToList();
    CurrentFolder = folder;
    StateHasChanged();
    // Ensure current folder reference is updated
    // _currentFolder = Folders.FirstOrDefault(f => f.Name == CurrentFolder.Name) ?? Folders.First();
    await InvokeAsync(StateHasChanged);
  }

  public void DetatchActions()
  {
    if (_isSubscribed)
    {
      StateService.OnFileExplorerClearSelection -= ClearSelection;
      StateService.OnCurrentFolderChanged -= HandleOnCurrentFolderChanged;
      StateService.OnToggleFileBrowserTree -= ToggleFileBrowserTree;
      _isSubscribed = false;
    }
  }

  public void Dispose()
  {
    DetatchActions();
  }

  private async Task OnCurrentPathChanged()
  {
    // Try to Query the Folder Api to get the folder with the exact Path
    // set the CurrentFolder to the returned folder from the Query
    await InvokeAsync(StateHasChanged);
  }
}

<style>
  :root {
    --file-explorer-Icon-text-color: var(--mud-palette-text-primary);
    --file-explorer-breadcrumb-height: 3.5rem;
    --mud-elevation-8 = var(--mud-elevation-2);
    --inner-shadow: inset 0px 2px 1px -1px rgba(0, 0, 0, 0.2), 0px 1px 1px 0px rgba(0, 0, 0, 0.14), 0px 1px 3px 0px rgba(0, 0, 0, 0.12);
  }
  /* This completely forces any element with mud-elevation-8 inside your menu to lose its shadow */
  .no-shadow-menu .mud-elevation-8,
  .no-shadow-menu.mud-elevation-8 {
    box-shadow: none !important;
  }

  .file-explorer-icon-highlited .mud-button-label {
    text-transform: none !important;
  }

    .file-explorer-icon-highlited .mud-button-label .mud-button-icon-start svg {
      color: var(--mud-palette-primary);
    }

  .file-explorer-icon-highlited .mud-popover {
    box-shadow: 0px 3px 1px -2px rgba(0, 0, 0, 0.2), 0px 2px 2px 0px rgba(0, 0, 0, 0.14), 0px 1px 5px 0px rgba(0, 0, 0, 0.12) !important;
  }

  .action-icon-button {
    padding: 6px 12px;
    color: var(--mud-palette-primary);
  }

  .action-icon-button {
    padding: 6px 12px;
    color: var(--mud-palette-primary);
  }

  .truncate {
    max-width: 150px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .panel-inner-shadow {
    box-shadow: inset 0px 3px 5px -1px rgba(0,0,0,0.2), inset 0px 5px 8px 0px rgba(0,0,0,0.14), inset 0px 1px 14px 0px rgba(0,0,0,0.12);
  }

  .first-panel-wrapper {
    height: unset !important;
  }

  .mud-spliter-divider {
    background-color: var(--mud-palette-background) !important;
  }

    .mud-spliter-divider:hover {
      background-color: var(--mud-palette-primary) !important;
    }

  .mud-split-panel-first {
    max-width: 500px !important;
  }
</style>